importance: 5

---

# プロパティをカウントする

オブジェクトの中のプロパティの数を返す関数 `count(obj)` を書いてください。

```js
let user = {
  name: 'John',
  age: 30
};

alert( count(user) ); // 2
```

できるだけ短いコードを作ってください。

P.S. シンボリックプロパティは無視し、"通常" のものだけをカウントしてください。
